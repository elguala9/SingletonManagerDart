name: singleton_manager_test
description: >
  Comprehensive test suite and utilities for the singleton_manager package.
  Includes fixtures, helpers, and integration tests for validating
  singleton manager functionality.

version: 0.1.0

resolution: workspace

publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  singleton_manager: ^0.1.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.24.0
